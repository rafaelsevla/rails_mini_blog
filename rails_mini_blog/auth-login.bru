meta {
  name: auth/login
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "joao@test.com",
    "password": "senha123"
  }
}

settings {
  encodeUrl: true
}
